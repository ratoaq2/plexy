---
paths:
  - "plexy/cli.py"
---

# CLI

The config file order and the parameter formats are in `docs/cli.md`. The README shows the `--help` output.
When you change an option, update `docs/cli.md` and `README.md` in the same commit.
