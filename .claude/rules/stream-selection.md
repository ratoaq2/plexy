---
paths:
  - "plexy/api.py"
---

# Stream selection

The rank of the streams decides what plexy writes to the user's Plex server. When you change the rank or the
watching preference logic, update `docs/stream-selection.md` and add a test for the new case.
